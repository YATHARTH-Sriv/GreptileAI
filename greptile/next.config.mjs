/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
            hostname :"www.greptile.com"
            }
        ]
    }
};

export default nextConfig;
